# Confluence configuration (official namespace)
[sources.atlassian_confluence]
base_url = "https://orbisinvestments.atlassian.net"
space_key = "INT"
expand = "body.storage,space,metadata.labels,ancestors,version"

# Legacy namespace still supported as fallback:
# [sources.confluence]

# AI configuration
[ai]
embedding_model = "sentence-transformers/all-MiniLM-L6-v2"
generation_model = "microsoft/phi-2"
