[project]
name = "feather-ai"
version = "0.1.0"
description = "Local-first knowledge engine for Confluence data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dagster",
    "dagster-dbt",
    "dagster-dlt",
    "dagster-webserver",
    "dlt[duckdb,parquet]",
    "dbt-duckdb",
    "motherduck",
    "requests",
    "sentence-transformers",
    "transformers",
    "torch",
    "accelerate",
    "html2text>=2025.4.15",
    "markdownify>=1.2.2",
]

[tool.hatch.build.targets.wheel]
packages = []
